module app.bench_fib

import ori.io = io

main()
    const n: int = 20000000
    var a: int = 0
    var b: int = 1
    var i: int = 0
    while i < n
        const t: int = a + b
        a = b
        b = t
        i = i + 1
    end
    io.print(f"{a}")
end
