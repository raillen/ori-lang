[package]
name = "web_session_sqlite"
version = "0.1.0"
entry = "src/lib.orl"
ori_version = "0.3.0"
description = "SQLite session store adapter for ori-web (B3) via use_custom_sessions"

# Optional default path — override in app ori.proj if needed.
[dependencies]
web = { path = "../ori-web", version = "0.1.0" }
sqlite = { path = "/home/raillen/Documentos/Projetos/ori-sqlite", version = "0.3.0" }
