module ori.doubly_linked_list

import ori.list = lists

public from_list[T](items: list[T]) -> ori.doubly_linked_list.DoublyLinkedList[T]
    var node: ori.doubly_linked_list.DoublyLinkedList[T] = ori.doubly_linked_list.new()
    var i: int = 0
    const n: int = lists.len(items)
    while i < n
        if some(item) = lists.try_get(items, i)
            ori.doubly_linked_list.push_back(node, item)
        end
        i = i + 1
    end
    return node
end

public front_or[T](node: ori.doubly_linked_list.DoublyLinkedList[T], fallback: T) -> T
    if some(value) = ori.doubly_linked_list.front(node)
        return value
    end
    return fallback
end

public back_or[T](node: ori.doubly_linked_list.DoublyLinkedList[T], fallback: T) -> T
    if some(value) = ori.doubly_linked_list.back(node)
        return value
    end
    return fallback
end

