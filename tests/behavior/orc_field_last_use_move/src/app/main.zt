namespace app.main

struct Box
    value: text
end

func main() -> int 
    const box: Box = Box(value: "zenith")
    const moved: text = box.value
    if len(moved) == 6
        return 0
    end
    return 1
end
