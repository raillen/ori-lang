namespace app.main

import std.mem as mem

enum Token
    Name(value: text)
    End
end

func main() -> int
    const token: Token = Token.Name(value: "zen")
    const copied: Token = mem.edit(token)
    return 0
end
