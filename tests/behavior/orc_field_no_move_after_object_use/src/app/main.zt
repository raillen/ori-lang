namespace app.main

struct Box
    value: text
end

func main() -> int 
    const box: Box = Box(value: "zenith")
    const alias: text = box.value
    if len(box.value) == 6 and len(alias) == 6
        return 0
    end
    return 1
end
