namespace app.main

func main() -> int
    const items: list<any<TextRepresentable>> = [1, true, 2.5, "ok"]
    var total: int = 0

    for item, index in items
        total = total + len(item.to_text()) + index
    end

    return total
end
