namespace app.main

func main() -> int 
    const original: text = "zenith"
    var alias: text = ""
    var count: int = 0
    while len(original) == 6 and count < 1 
        alias = original
        count = count + 1
    end
    if len(alias) == 6 
        return 0
    end
    return 1
end
