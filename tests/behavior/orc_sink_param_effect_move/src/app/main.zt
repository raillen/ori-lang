namespace app.main

func consume_text(value: text) -> int 
    const owned: text = value
    return len(owned) + 1
end

func main() -> int 
    const original: text = "zenith"
    consume_text(original)
    return 0
end
