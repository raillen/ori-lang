namespace app.main

enum Color
    Red
    Green
    Blue
end

func main() -> int
    const c: Color = Color.Red
    match c
        case Color.Red ->
            return 0
        case .. ->
            return 1
    end
end
