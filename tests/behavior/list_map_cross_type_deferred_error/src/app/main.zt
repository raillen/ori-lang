namespace app.main

import std.list as list

func main() -> int
    const values: list<int> = [1, 2]
    const labels: list<text> = list.map(values, func(value: int) => "item")
    return len(labels)
end
