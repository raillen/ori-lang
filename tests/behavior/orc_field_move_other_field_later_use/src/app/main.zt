namespace app.main

struct Box
    value: text
    marker: int
end

func main() -> int 
    const box: Box = Box(value: "zenith", marker: 1)
    const moved: text = box.value
    if box.marker == 1 and len(moved) == 6
        return 0
    end
    return 1
end
