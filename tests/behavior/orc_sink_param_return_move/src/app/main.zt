namespace app.main

func consume_text(value: text) -> int 
    const owned: text = value
    return len(owned) + 1
end

func main() -> int
    const original: text = "zenith"
    const score: int = consume_text(original)
    if score == 7
        return 0
    end
    return 1
end
