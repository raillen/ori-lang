namespace app.main

import std.mem as mem
import std.set as set

func main() -> int
    const tuple_keys: set<tuple<int, text>> = set.empty()
    const copied: set<tuple<int, text>> = mem.edit(tuple_keys)
    return set.len(copied)
end
