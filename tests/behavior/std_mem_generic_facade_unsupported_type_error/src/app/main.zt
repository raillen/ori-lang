namespace app.main

import std.mem as mem

func main() -> int
    const values: list<text> = ["one"]
    const pair: tuple<int, list<text>> = (1, values)
    const owned: tuple<int, list<text>> = mem.own(pair)
    return len(owned.1)
end
