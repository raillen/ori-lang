namespace app.tests

import std.test as test

attr test
attr skip("tracked by attr skip")
public func skipped_case() -> void
    test.fail("attr skip should prevent this body from running")
    return
end
