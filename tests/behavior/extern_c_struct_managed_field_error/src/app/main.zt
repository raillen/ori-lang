namespace app.main

attr repr("c")
struct Label
    name: text
end

extern c
    func zt_ffi_label_code(label: Label) -> int
end

public func main() -> int
    const label: Label = Label(name: "alpha")
    return zt_ffi_label_code(label)
end
